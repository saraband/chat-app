import React from 'react'
import { connect } from 'react-redux'

export default class RoomComponent extends React.PureComponent {
  constructor(props) {
    super(props)
  }

  render() {
    return(
      <p>RoomComponent</p>
    )
  }
} 